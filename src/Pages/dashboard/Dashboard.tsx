import { Button, Column, Grid, Modal, Tag, TextInput, Tile } from "@carbon/react";

const Dashboard = () => {
     return   (
         <Grid>
             
             <Button
             kind="danger"
             >Delete</Button>
             <Modal></Modal>
          
         </Grid>
            
    );
}
export default Dashboard;   